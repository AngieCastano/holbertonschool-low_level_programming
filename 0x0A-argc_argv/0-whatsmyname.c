#include <stdio.h>
/**
 * main - prints fil's name
 * @argc: Qtity of argumets
 * @argv: Vector of arguments
 * Return: 0 if succes
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
