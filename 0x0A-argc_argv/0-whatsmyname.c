#include <stdio.h>
/**
 * main- prints the progrms name
 * @argc: quantity of argumets
 * @argv: vector of arguments
 * Return: 0 if succes
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
