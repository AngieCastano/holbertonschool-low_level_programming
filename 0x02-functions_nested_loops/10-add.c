#include "holberton.h"
/**
 * add - tells the absolute value of a number
 * @n: Integer as a number
 * @f: Integer as a number
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int add(int n, int f)
{
	return (n + f);
}
