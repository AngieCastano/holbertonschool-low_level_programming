#include "holberton.h"
/**
 * print_sign - tells if a character i lower or not
 * @n: Letter
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int print_sign(int n)
{
	if (n < 0)
	{
		n = n * -1
		return (n);
	}
	else
		return (n);
}
