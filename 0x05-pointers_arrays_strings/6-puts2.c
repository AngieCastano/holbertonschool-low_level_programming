#include "holberton.h"
/**
 * puts2 - printing every other character of a function
 * @str: string to print in odd
 *
 */
void puts2(char *str)
{
	int i, j;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	for (j = 0; j <= (i - 1); j + 2)
	{
		_putchar(s[j]);
	}
	_putcher('\n');
}
