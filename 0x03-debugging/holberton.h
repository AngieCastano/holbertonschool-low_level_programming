#include "holberton.h"

/**
 * main - print Holberon
 * On error, -1 is returned, and errno is set appropriately.
 * Return: On succes 1.
 */
int main(void)
{
	int largest_number(int a, int b, int c);
	return (0);
}
