#include <stdio.h>
/**
 * main - here writing my first printing function
 *
 * Return: Succes(0)
 */

int main(void)
{
	char *charname = "\"Programming is like building a multilingual puzzle";

	puts(charname);
	return (0);
}
