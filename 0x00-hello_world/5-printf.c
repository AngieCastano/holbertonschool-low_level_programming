#include <stdio.h>
/**
 * main - printf
 *
 * Return: Succes(0) Error(1)
 */

int main(void)
{
	char *star = "with proper grammar, but the outcome is a piece of art,"

	printf("%s\n", star);
	return (0);
}
